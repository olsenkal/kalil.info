# Default Open Graph image

`og-default.html` is the source for `static/og-default.png` (1200x630). The K is an SVG path (same outline as `static/favicon.svg`), so only the Georgia text depends on installed fonts.

Regenerate from the repo root (macOS, Chrome):

    ./scripts/og/build.sh
